import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#333",
    paddingVertical: 2,
    paddingHorizontal: 8,
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 999,
  },
  text: {
    fontSize: 12,
    color: "#D9D9D9",
    fontWeight: "700",
  },
});
